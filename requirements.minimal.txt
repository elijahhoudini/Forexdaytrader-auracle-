# Minimal requirements for AURACLE to run
# These are the absolute essentials

# Core dependencies
requests>=2.28.0
aiohttp>=3.8.0

# Optional but recommended
python-telegram-bot>=20.0
python-dotenv>=1.0.0
colorama>=0.4.6

# Solana (optional - will use minimal implementation if not available)
solana>=0.34.0
solders>=0.21.0